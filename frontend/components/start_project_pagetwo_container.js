import {connect} from 'react-redux';
import StartProjectPageTwo from './start_project_pagetwo';

const mapStateToProps = state => {
  return {

  };
};

const mapDispatchToProps = dispatch => {
  return {
    
  };
};

export default connect(mapStateToProps, mapDispatchToProps)(StartProjectPageTwo);
