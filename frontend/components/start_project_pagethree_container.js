import {connect} from 'react-redux';
import StartProjectPageThree from './start_project_pagethree';

const mapStateToProps = state => {
  return {

  };
};

const mapDispatchToProps = dispatch => {
  return {

  };
};

export default connect(mapStateToProps, mapDispatchToProps)(StartProjectPageThree);
